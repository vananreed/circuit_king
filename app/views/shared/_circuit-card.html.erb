<!-- recieves circuit object in circuit variable -->

<div class="circuit-card">
  <h3><%= circuit.name  %></h3>
  <% circuit.exercises.each do |e| %>
    <div><%= e.name %>  |  <%= link_to "->", e %></div>
  <% end %>

  <%= render 'shared/workout-length-modal', circuit: circuit %>

</div>
